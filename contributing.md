# Contributing

Testing Locally:

```shell
asdf plugin test terramate-catalyst https://github.com/terramate-io/asdf-terramate-catalyst.git "terramate version"
```

Tests are automatically run in GitHub Actions on push and PR.
